run:
  skip-dirs:
    - temp
    - external

linters-settings:
  golint:
    min-confidence: 0.3

  gocyclo:
    max-complexity: 15

  errcheck:
    exclude: cilint.txt

linters:
  disable:
    - errcheck